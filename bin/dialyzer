#!/bin/sh

MIX_ENV="$1"
if [ -z "${MIX_ENV}" ]; then
    MIX_ENV=dev
fi
export MIX_ENV

mix do compile, dialyzer | perl -p0e 's/.*?Proceeding with analysis...//s' | perl -p0e 's/Unknown functions:.*//s'
