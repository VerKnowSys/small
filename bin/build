#!/bin/sh

MIX_ENV="$1"
if [ -z "${MIX_ENV}" ]; then
    MIX_ENV=dev
fi
export MIX_ENV

if [ "${1}" = "q" ]; then
    MIX_ENV=dev
    mix do compile
else
    mix do deps.compile, compile, escript.build
fi
