#!/bin/sh

MIX_ENV="$1"
if [ -z "${MIX_ENV}" ]; then
    MIX_ENV=dev
fi
export MIX_ENV

LOCAL_DEST="/usr/local/bin"
for entry in small deps/fs/priv/mac_listener bin/reattach-to-user-namespace; do
    install ${entry} ${LOCAL_DEST}
done
